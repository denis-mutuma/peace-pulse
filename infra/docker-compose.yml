services:
  api:
    build:
      context: ..
      dockerfile: infra/python.Dockerfile
    command: uv run python services/api/server.py
    ports:
      - "8080:8080"
    volumes:
      - ../data:/app/data
